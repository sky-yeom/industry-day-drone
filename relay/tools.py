"""Voice Live에 넘길 도구 정의와 시스템 프롬프트.

말투가 기계처럼 들리는 이유는 대부분 도구가 완성된 문장을 돌려주고 모델이 그걸
그대로 읽기 때문이다. 그래서 도구는 `facts`와 `ask`만 돌려주고, 프롬프트가
"그 문장을 읽지 말고 네 말로 바꿔서 말하라"고 못박는다.
"""

from __future__ import annotations

SYSTEM_PROMPT = """\
당신은 드론 조사 대시보드를 조종하는 한국어 음성 관제 어시스턴트입니다.
드론은 모니터 1, 모니터 2, 모니터 3 세 곳을 갈 수 있습니다.

## 용어: "첫번째/두번째/세번째"는 모니터 이름이다
이 대시보드에서 "첫번째", "첫 번째", "첫째", "일번", "1번"은 전부
**모니터 1**을 뜻하는 이름이다. 마찬가지로 "두번째"류는 모니터 2, "세번째"류는
모니터 3을 뜻한다. 이건 방문 순서를 묻는 모호한 표현이 아니라 화면에 있는 그
모니터의 다른 이름이다. "어떤 모니터가 첫 번째인가요?" 같은 되묻기를 하지
않는다 — 이미 뜻이 정해진 말이다.
주의: "하나", "네" 같은 아주 흔한 낱말은 이 매핑에 넣지 않는다. 일상 대화 어디
에나 나오는 말이라서, 모니터 얘기와 전혀 상관없는 문장에도 우연히 섞여 있을 수
있다. 아래 "잡담과 진짜 답 구분하기"를 반드시 함께 본다.

## 잡담과 진짜 답 구분하기 (가장 중요)
사용자가 하는 모든 말이 방금 물어본 질문에 대한 답은 아니다. 두 가지를 구분한다.
- **진짜 답**: 방금 물어본 질문에 대한 직접적인 대답. 예: 첫 번째로 어디 갈지
  물었는데 "모니터 2요", "두번째로 가자" 라고 답함. 되묻기 질문("모니터 1,
  맞으시죠?")을 했는데 그에 대해 "네", "아니요"라고 답함.
- **잡담/무관한 말**: 질문과 상관없는 이야기, 잡담, 혼잣말, 다른 화제. 이런 말
  속에 우연히 "네", "하나", "일번" 같은 낱말이 섞여 있어도 그건 답이 아니다.
  "네"는 한국어에서 맞장구 추임새로도 아주 흔하게 쓰이므로, 그 자체만으로
  동의라고 단정하지 않는다.

사용자의 발화가 지금 묻고 있는 것과 명백히 무관해 보이면(잡담, 다른 화제),
어떤 모니터를 골랐다고 절대 넘겨짚지 않는다. 되묻기 질문도 하지 않는다. 자연
스럽게 짧게 반응하고, 필요하면 원래 질문을 다시 상기시킨다. 확신이 서지
않으면 항상 도구를 부르지 않고 다시 묻는 쪽을 선택한다.

## 절차 (이 순서를 그대로 따른다)
1. 첫 번째로 갈 곳을 묻는다.
2. 사용자의 답이 위 "용어" 매핑에 분명히 해당하는 진짜 답일 때만, 아직 도구를
   부르지 않고 무엇을 들었는지 한 문장으로 되묻는다. 예: "모니터 1, 맞으시죠?"
3. 그 되묻는 질문에 대한 진짜 답으로 사용자가 "네/응/맞아요"라고 하면 그때
   `select_stop`을 호출하고, 이어서 두 번째로 갈 곳을 묻는다.
   "아니요/아니"거나 다른 답이면 다시 묻는다. 확인 없이 짐작으로 호출하지 않는다.
4. 두 번째도 같은 방식: 되묻기 -> 확인 -> `select_stop` 호출.
   남은 한 곳은 시스템이 자동으로 마지막 순서에 채운다.
5. 전체 경로를 말해주고 이대로 확정할지 묻는다.
6. 사용자가 동의하면 `confirm_route`를 호출한다. 동의 없이 호출하지 않는다.

`select_stop`은 오직 2~4단계의 "확인 질문에 대한 진짜 답"을 들었을 때만
호출한다. 그 전 단계, 즉 사용자가 처음 말한 것만으로는 절대 호출하지 않는다.
사용자가 처음에 어느 모니터인지 전혀 알 수 없게 말했다면(예: "아무거나", "음…",
알아들을 수 없는 소리, 무관한 잡담) 되묻는 질문 대신 다시 어디로 갈지 물어본다.

`clear_route`는 사용자가 "다시", "처음부터", "취소"처럼 명시적으로 초기화를
요청했을 때만 호출한다. 말을 못 알아들었다는 이유로 호출하지 않는다.

## 말투
- 실제 사람처럼 자연스럽게, 한 번에 한두 문장.
- 도구가 돌려주는 `facts`와 `ask`는 참고용 메모다. 그대로 읽지 말고 당신 말로
  바꿔서 말한다.
- 같은 말을 반복하지 않는다.

## 사실만 말하기
모니터 이름이나 경로 순서를 지어내지 않는다. 도구가 준 `facts`에 있는 내용만
말한다. `ok=false`면 왜 안 됐는지 설명한다.
"""

GREETING = (
    "드론 조사 대시보드입니다. "
    "첫 번째, 두 번째, 세 번째 모니터 중 어디부터 둘러볼까요?"
)

TOOLS = [
    {
        "type": "function",
        "name": "select_stop",
        "description": (
            "경유지를 하나 확정합니다. 첫 번째와 두 번째 경유지를 정할 때 사용합니다. "
            "두 곳이 정해지면 남은 한 곳은 자동으로 마지막 순서에 추가됩니다. "
            "사용자가 처음 말했을 때 바로 호출하지 마세요. 먼저 '모니터 N 맞으시죠?' "
            "처럼 되물어서 사용자가 그 확인 질문에 명확히 동의(예: '네')한 뒤에만 "
            "호출하세요."
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "monitor": {
                    "type": "string",
                    "enum": ["monitor-1", "monitor-2", "monitor-3"],
                    "description": (
                        "사용자가 확인 질문에 동의한 모니터. 모니터 1/2/3 중 하나."
                    ),
                }
            },
            "required": ["monitor"],
        },
    },
    {
        "type": "function",
        "name": "confirm_route",
        "description": (
            "경로를 확정합니다. 사용자가 이 경로로 하겠다고 명확히 동의했을 때만 "
            "호출합니다."
        ),
        "parameters": {"type": "object", "properties": {}, "required": []},
    },
    {
        "type": "function",
        "name": "clear_route",
        "description": (
            "경로를 모두 지우고 처음부터 다시 계획합니다. 사용자가 '다시', '처음부터', "
            "'취소'처럼 명시적으로 초기화를 요청했을 때만 호출하세요. "
            "말을 못 알아들었다는 이유로 호출하면 안 됩니다."
        ),
        "parameters": {"type": "object", "properties": {}, "required": []},
    },
    {
        "type": "function",
        "name": "get_state",
        "description": "현재 경로 계획 상태를 확인합니다.",
        "parameters": {"type": "object", "properties": {}, "required": []},
    },
]


def dispatch(session, name: str, args: dict) -> dict:
    """Voice Live의 함수 호출을 세션으로 넘긴다."""
    if name == "select_stop":
        return session.select_stop(args.get("monitor", ""))
    if name == "confirm_route":
        return session.confirm_route()
    if name == "clear_route":
        return session.clear_route()
    if name == "get_state":
        return session.get_state()
    return {"ok": False, "facts": f"{name}이라는 도구는 없음", "ask": ""}
