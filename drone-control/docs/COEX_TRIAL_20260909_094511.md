# COEX 무태그 시험: 상승 시작 직후 중단

- 로그: `pc/logs/coex/20260909T094511-87577037.jsonl`
- 시작 배터리78%, 자동 이륙 약1.2m. 좌우 이동 명령0회.
- 첫 상승 명령 up_mps=0.18 이후 `DUPLICATE_OBSERVATION`으로 중단.
- RC 인계 확인 및 실제 IsFlying=false / AreMotorsOn=false 착륙 확인 완료.
- 원인: 이 PC의 Python3.12 `time.monotonic()`은 GetTickCount64, 해상도15.625ms. 정상 ACK seq68과69가 같은 tick에 들어와 서로 다른 관측의 수신 시각이 같아짐.
- 수정: COEX I/O의 수신·GET·deadline 및 runner clock을 동일한 `time.perf_counter()`(QueryPerformanceCounter, 이 PC 해상도100ns)로 변경. 중복·신선도·적분 공백 검사는 유지.
- 앱/펌웨어/센서 문제로 분류하지 않음. 7연속 카운트 제외.
- 사용자가 착륙 후 다음 실행을 명시적으로 지시함. 자동 재시도는 하지 않음.
